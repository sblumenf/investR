# Extracted from test-mod_position_risk.R:287

# test -------------------------------------------------------------------------
mock_results <- list(monte_carlo = NULL)
dist_html <- as.character(create_distribution_tab(mock_results))
