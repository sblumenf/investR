% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_dividend_capture.R
\name{should_filter_dividend_opportunity}
\alias{should_filter_dividend_opportunity}
\title{Filter dividend capture opportunity based on quality criteria}
\usage{
should_filter_dividend_opportunity(
  stats,
  ticker,
  min_success_rate = 70,
  exclude_negative_returns = TRUE
)
}
\arguments{
\item{stats}{Tibble row with analysis statistics}

\item{ticker}{Character ticker symbol (for logging)}

\item{min_success_rate}{Numeric minimum success rate threshold (percentage, 0-100)}

\item{exclude_negative_returns}{Logical whether to filter negative returns}
}
\value{
Logical TRUE if opportunity should be filtered out, FALSE if it passes
}
\description{
Applies standard filters to dividend capture opportunities:
1. Negative expected returns (avg_return <= 0)
2. Low probability of success (success_rate < threshold)
}
\details{
This is a shared function following DRY principles - used by all
dividend capture strategies (weekly, monthly, high-yield, etc.)
}
\examples{
\dontrun{
  # After calculating statistics
  stats <- calculate_weekly_statistics(...)

  if (should_filter_dividend_opportunity(stats, "QQQY", 70, TRUE)) {
    return(NULL)  # Filtered out
  }
}
}
