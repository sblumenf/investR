% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_covered_calls_shared.R
\name{analyze_covered_calls_generic}
\alias{analyze_covered_calls_generic}
\title{Generic covered calls analysis orchestrator}
\usage{
analyze_covered_calls_generic(
  stock_universe,
  strategy_name,
  strike_threshold_pct = 0.8,
  min_days = NULL,
  max_days = NULL,
  expiry_month = NULL,
  target_days = NULL,
  max_workers = 10,
  result_flags = list()
)
}
\arguments{
\item{stock_universe}{Character vector of tickers to analyze}

\item{strategy_name}{Name for logging (e.g., "Dividend Aristocrats")}

\item{strike_threshold_pct}{Maximum strike as % of current price}

\item{min_days}{Minimum days to expiry (NULL = no minimum)}

\item{max_days}{Maximum days to expiry (NULL = longest available)}

\item{expiry_month}{Target expiry month (1-12, NULL for any)}

\item{max_workers}{Number of parallel workers}

\item{result_flags}{Named list of additional flags to add to results}
}
\value{
Tibble with all opportunities sorted by annualized return
}
\description{
Orchestrates the complete covered call analysis workflow for any stock universe.
This is the main entry point that coordinates parallel processing, logging,
and result aggregation.
}
\examples{
\dontrun{
  # Analyze custom stock universe
  my_stocks <- c("AAPL", "MSFT", "GOOGL")
  results <- analyze_covered_calls_generic(
    stock_universe = my_stocks,
    strategy_name = "Tech Giants",
    strike_threshold_pct = 0.80,
    min_days = 60,
    max_days = 90,
    expiry_month = 6,
    max_workers = 4,
    result_flags = list(is_tech = TRUE)
  )
}
}
