% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_etf_yfscreen_analysis.R
\name{analyze_etf_covered_calls_yfscreen}
\alias{analyze_etf_covered_calls_yfscreen}
\title{Analyze ETFs from yfscreen for covered call opportunities}
\usage{
analyze_etf_covered_calls_yfscreen(
  dividend_yield_min = NULL,
  dividend_yield_max = NULL,
  dividend_filter = "all",
  market_cap_min = 1e+09,
  market_cap_max = 1e+11,
  top_n = 50,
  strike_threshold_pct = ETF_SCREENER_CONFIG$strike_threshold_pct,
  min_days = ETF_SCREENER_CONFIG$min_days,
  max_days = ETF_SCREENER_CONFIG$max_days,
  expiry_month = NULL,
  max_workers = ETF_SCREENER_CONFIG$max_workers
)
}
\arguments{
\item{dividend_yield_min}{Minimum dividend yield (default NULL)}

\item{dividend_yield_max}{Maximum dividend yield (default NULL)}

\item{dividend_filter}{Dividend filter type: "all", "dividend_paying", or "zero_dividend" (default "all")}

\item{market_cap_min}{Minimum market cap in dollars (default 1 billion)}

\item{market_cap_max}{Maximum market cap in dollars (default 100 billion)}

\item{top_n}{Maximum number of ETFs to analyze, sorted by volume (default 50)}

\item{strike_threshold_pct}{Maximum strike as % of current price (default 0.85)}

\item{min_days}{Minimum days to expiry (default 45)}

\item{max_days}{Maximum days to expiry (default 120)}

\item{expiry_month}{Target expiry month (1-12, NULL for any)}

\item{max_workers}{Number of parallel workers for processing}
}
\value{
Tibble with all opportunities sorted by annualized return
}
\description{
Uses yfscreen package to filter ETFs by expense ratio, dividend yield, and
net assets, then analyzes the filtered ETFs for deep ITM covered call
opportunities. Provides dynamic, real-time ETF screening based on current
market data.
}
\details{
Key differences from static ETF lists:
- Dynamic filtering based on real-time ETF characteristics
- Customizable screening criteria (expense ratio, yield, assets)
- Automatically includes new ETFs that meet criteria
- Excludes ETFs that no longer meet filtering thresholds
}
\examples{
\dontrun{
  # Analyze ETFs with default screening parameters
  results <- analyze_etf_covered_calls_yfscreen()

  # Analyze with custom screening criteria
  results <- analyze_etf_covered_calls_yfscreen(
    dividend_yield_min = 3,
    dividend_yield_max = 8,
    dividend_filter = "dividend_paying",
    market_cap_min = 10e9,
    market_cap_max = 50e9,
    strike_threshold_pct = 0.90,
    min_days = 60,
    max_days = 90
  )

  # Zero-dividend ETF screening
  results <- analyze_etf_covered_calls_yfscreen(
    dividend_filter = "zero_dividend",
    market_cap_min = 5e9,
    market_cap_max = 100e9,
    top_n = 30
  )
}
}
