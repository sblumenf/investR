% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_zero_dividend_analysis.R
\name{analyze_zero_dividend_etfs}
\alias{analyze_zero_dividend_etfs}
\title{Analyze liquid ETFs for zero-dividend covered call opportunities}
\usage{
analyze_zero_dividend_etfs(
  min_market_cap = COLLAR_CONFIG$min_market_cap,
  min_avg_volume = COLLAR_CONFIG$min_avg_volume,
  strike_threshold_pct = ZERO_DIVIDEND_CONFIG$strike_threshold_pct,
  min_days = ZERO_DIVIDEND_CONFIG$min_days,
  max_days = ZERO_DIVIDEND_CONFIG$max_days,
  expiry_month = NULL,
  max_workers = ZERO_DIVIDEND_CONFIG$max_workers
)
}
\arguments{
\item{min_market_cap}{Minimum market capitalization (default from COLLAR_CONFIG)}

\item{min_avg_volume}{Minimum average daily volume (default from COLLAR_CONFIG)}

\item{strike_threshold_pct}{Maximum strike as % of current price (default 0.85)}

\item{min_days}{Minimum days to expiry (default 45)}

\item{max_days}{Maximum days to expiry (default 120)}

\item{expiry_month}{Target expiry month (1-12, NULL for any)}

\item{max_workers}{Number of parallel workers for processing}
}
\value{
Tibble with all opportunities sorted by annualized return
}
\description{
Analyzes highly liquid ETFs for deep ITM covered call opportunities using
zero-dividend strategy parameters. Uses the same curated ETF list as the
collar strategy (50+ liquid ETFs across index, sector, bond, commodity, etc.).
}
\details{
Note: Many liquid ETFs pay dividends (e.g., SPY, VOO, VYM, SCHD). This is
acceptable as the strategy name refers to the deep ITM covered call approach,
not the dividend status of the underlying.
}
\examples{
\dontrun{
  # Analyze liquid ETFs with default parameters
  results <- analyze_zero_dividend_etfs()

  # Analyze with custom parameters
  results <- analyze_zero_dividend_etfs(
    strike_threshold_pct = 0.90,
    min_days = 60,
    max_days = 90
  )
}
}
