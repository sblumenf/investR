% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_stock_universe.R
\name{get_zero_dividend_stocks}
\alias{get_zero_dividend_stocks}
\title{Get zero-dividend S&P 500 stocks with caching}
\usage{
get_zero_dividend_stocks(limit = NULL, max_workers = 10)
}
\arguments{
\item{limit}{Optional limit on number of stocks to check (for testing)}

\item{max_workers}{Number of parallel workers to use (default 4)}
}
\value{
Character vector of zero-dividend ticker symbols
}
\description{
Filters S&P 500 for stocks that don't pay dividends. Results are cached
for 30 days to avoid expensive repeated scans.
}
\examples{
\dontrun{
  zero_div_stocks <- get_zero_dividend_stocks()
  length(zero_div_stocks)  # Should be ~80-100

  # Test with limit
  zero_div_stocks <- get_zero_dividend_stocks(limit = 50)
}
}
