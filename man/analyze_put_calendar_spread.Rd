% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_put_calendar_spread.R
\name{analyze_put_calendar_spread}
\alias{analyze_put_calendar_spread}
\title{Analyze dividend aristocrats for put calendar spread opportunities}
\usage{
analyze_put_calendar_spread(
  limit = NULL,
  strike_pct = PUT_CALENDAR_SPREAD_CONFIG$strike_pct,
  short_min_days = PUT_CALENDAR_SPREAD_CONFIG$short_expiry_min_days,
  short_max_days = PUT_CALENDAR_SPREAD_CONFIG$short_expiry_max_days,
  long_min_days = PUT_CALENDAR_SPREAD_CONFIG$long_expiry_min_days,
  long_max_days = PUT_CALENDAR_SPREAD_CONFIG$long_expiry_max_days,
  max_workers = PUT_CALENDAR_SPREAD_CONFIG$max_workers
)
}
\arguments{
\item{limit}{Optional limit on number of stocks to analyze}

\item{strike_pct}{Target strike as % of current price (default 0.95 = ATM)}

\item{short_min_days}{Minimum days for short leg (default 20)}

\item{short_max_days}{Maximum days for short leg (default 45)}

\item{long_min_days}{Minimum days for long leg (default 50)}

\item{long_max_days}{Maximum days for long leg (default 120)}

\item{max_workers}{Number of parallel workers}
}
\value{
Tibble with all opportunities sorted by opportunity score
}
\description{
Main entry point for put calendar spread analysis on dividend aristocrats.
Finds optimal short/long put pairs to capitalize on theta decay differential.
}
\examples{
\dontrun{
  # Analyze all dividend aristocrats
  results <- analyze_put_calendar_spread()

  # Analyze with custom parameters
  results <- analyze_put_calendar_spread(
    strike_pct = 0.90,
    short_max_days = 30,
    long_min_days = 60,
    max_workers = 4
  )

  # Quick test with limit
  results <- analyze_put_calendar_spread(limit = 5)
}
}
