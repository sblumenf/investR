% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_cash_secured_puts.R
\name{analyze_cash_secured_puts}
\alias{analyze_cash_secured_puts}
\title{Process stocks in parallel for put analysis}
\usage{
analyze_cash_secured_puts(
  limit = NULL,
  strike_threshold_pct = CASH_SECURED_PUTS_CONFIG$strike_threshold_pct,
  min_days = CASH_SECURED_PUTS_CONFIG$min_days,
  max_days = CASH_SECURED_PUTS_CONFIG$max_days,
  expiry_month = NULL,
  max_workers = CASH_SECURED_PUTS_CONFIG$max_workers
)
}
\arguments{
\item{limit}{Optional limit on number of stocks to analyze}

\item{strike_threshold_pct}{Minimum strike as % of current price (default 0.95)
Higher % = more conservative (closer to ATM)
Lower % = more aggressive (deeper OTM)}

\item{min_days}{Minimum days to expiry (default 45)}

\item{max_days}{Maximum days to expiry (default 120)}

\item{expiry_month}{Target expiry month (1-12, NULL for any)}

\item{max_workers}{Number of parallel workers}
}
\value{
Tibble with all opportunities sorted by annualized return
}
\description{
Analyze dividend aristocrats for cash-secured put opportunities
}
\details{
Analyzes dividend aristocrats for ATM/OTM cash-secured put opportunities.
Targets high-quality dividend stocks with put options priced above fair value.
}
\examples{
\dontrun{
  # Analyze all dividend aristocrats
  results <- analyze_cash_secured_puts()

  # Analyze with custom parameters
  results <- analyze_cash_secured_puts(
    strike_threshold_pct = 0.90,
    min_days = 60,
    max_workers = 4
  )
}
}
