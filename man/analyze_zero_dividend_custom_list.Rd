% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_zero_dividend_analysis.R
\name{analyze_zero_dividend_custom_list}
\alias{analyze_zero_dividend_custom_list}
\title{Analyze custom ticker list for zero-dividend covered call opportunities}
\usage{
analyze_zero_dividend_custom_list(
  list_type,
  strike_threshold_pct = ZERO_DIVIDEND_CONFIG$strike_threshold_pct,
  min_days = ZERO_DIVIDEND_CONFIG$min_days,
  max_days = ZERO_DIVIDEND_CONFIG$max_days,
  expiry_month = NULL,
  max_workers = ZERO_DIVIDEND_CONFIG$max_workers
)
}
\arguments{
\item{list_type}{Type of custom list: "overbought", "oversold", "most_shorted", "leveraged_2x", or "leveraged_3x"}

\item{strike_threshold_pct}{Maximum strike as % of current price (default 0.85)}

\item{min_days}{Minimum days to expiry (default 45)}

\item{max_days}{Maximum days to expiry (default 120)}

\item{expiry_month}{Target expiry month (1-12, NULL for any)}

\item{max_workers}{Number of parallel workers for processing}
}
\value{
Tibble with all opportunities sorted by annualized return
}
\description{
Analyzes custom ticker lists (overbought, oversold, most shorted, leveraged ETFs)
for deep ITM covered call opportunities using zero-dividend strategy parameters.
}
\examples{
\dontrun{
  # Analyze overbought stocks
  results <- analyze_zero_dividend_custom_list("overbought")

  # Analyze 2x leveraged ETFs with custom parameters
  results <- analyze_zero_dividend_custom_list(
    "leveraged_2x",
    strike_threshold_pct = 0.90,
    min_days = 60,
    max_days = 90
  )
}
}
