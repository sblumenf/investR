% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_ui_components.R
\name{create_strategy_opportunity_card}
\alias{create_strategy_opportunity_card}
\title{Create a generic strategy opportunity card}
\usage{
create_strategy_opportunity_card(
  row,
  primary_text,
  secondary_text,
  sections,
  ns = NULL,
  risk_id = NULL,
  include_risk_button = TRUE,
  card_class = "opportunity-card"
)
}
\arguments{
\item{row}{Single-row data frame with opportunity data}

\item{primary_text}{Card header primary text (e.g., "Company Name (TICKER)")}

\item{secondary_text}{Card header secondary text (e.g., "$XX.XX")}

\item{sections}{List of section configurations. Each section is a list with:
- title: Section title string
- is_open: Logical, should section be open by default
- metrics: List of metric specifications, each with:
  - label: Metric label string
  - value: Formatted value string
  - is_primary: Logical, use primary styling (default FALSE)
  - is_negative: Logical, use negative/red styling (default FALSE)}

\item{ns}{Optional namespace function for risk button (from module)}

\item{risk_id}{Optional risk module ID for risk analysis button}

\item{include_risk_button}{Logical, whether to include risk analysis button (default TRUE)}

\item{card_class}{CSS class for card styling (default "opportunity-card")}
}
\value{
A bslib card component with standardized structure
}
\description{
Reusable card builder for all strategy opportunity displays. Provides
consistent structure across strategies while allowing strategy-specific
content through section configuration.
}
\examples{
\dontrun{
  sections <- list(
    list(
      title = "Quick Overview",
      is_open = TRUE,
      metrics = list(
        list(label = "Return", value = "10.5\%", is_primary = TRUE),
        list(label = "Days", value = "45")
      )
    )
  )
  create_strategy_opportunity_card(
    row = opportunity_data,
    primary_text = "Apple Inc. (AAPL)",
    secondary_text = "$150.00",
    sections = sections
  )
}
}
