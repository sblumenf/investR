% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_sp500_cash_secured_puts.R
\name{analyze_sp500_cash_secured_puts}
\alias{analyze_sp500_cash_secured_puts}
\title{S&P 500 Cash-Secured Puts Analysis}
\usage{
analyze_sp500_cash_secured_puts(
  dividend_filter = "all",
  limit = NULL,
  strike_threshold_pct = CASH_SECURED_PUTS_CONFIG$strike_threshold_pct,
  min_days = CASH_SECURED_PUTS_CONFIG$min_days,
  max_days = CASH_SECURED_PUTS_CONFIG$max_days,
  expiry_month = NULL,
  max_workers = CASH_SECURED_PUTS_CONFIG$max_workers
)
}
\arguments{
\item{dividend_filter}{Dividend filter type: "all", "dividend_paying", or "zero_dividend"}

\item{limit}{Maximum number of stocks to analyze (NULL for no limit). Applied only when
dividend_filter is "dividend_paying" or "zero_dividend". Default NULL.}

\item{strike_threshold_pct}{Minimum strike as % of current price (default from config)}

\item{min_days}{Minimum days to expiry (default from config)}

\item{max_days}{Maximum days to expiry (default from config)}

\item{expiry_month}{Target expiry month (1-12, NULL for any)}

\item{max_workers}{Number of parallel workers (default from config)}
}
\value{
Tibble with cash-secured put opportunities sorted by annualized return
}
\description{
Analyzes S&P 500 stocks for cash-secured put opportunities with flexible
dividend filtering. Can analyze all S&P 500 stocks, only dividend-paying
stocks, or only zero-dividend growth stocks.
}
\details{
Key features:
- Flexible dividend filtering (all, dividend-paying, zero-dividend)
- Uses established S&P 500 stock universes
- Optional limit parameter for faster analysis
- Comprehensive result flags for strategy identification
}
\examples{
\dontrun{
  # Analyze all S&P 500 stocks
  results <- analyze_sp500_cash_secured_puts(
    dividend_filter = "all"
  )

  # Analyze top 50 dividend-paying stocks only
  results <- analyze_sp500_cash_secured_puts(
    dividend_filter = "dividend_paying",
    limit = 50,
    strike_threshold_pct = 0.85,
    min_days = 30,
    max_days = 45
  )

  # Analyze zero-dividend growth stocks
  results <- analyze_sp500_cash_secured_puts(
    dividend_filter = "zero_dividend",
    limit = 25,
    strike_threshold_pct = 0.90
  )
}
}
