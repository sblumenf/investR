% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_etf_cash_secured_puts_yfscreen.R
\name{analyze_etf_cash_secured_puts_yfscreen}
\alias{analyze_etf_cash_secured_puts_yfscreen}
\title{ETF Cash-Secured Puts Analysis (yfscreen Filtered)}
\usage{
analyze_etf_cash_secured_puts_yfscreen(
  dividend_yield_min = NULL,
  dividend_yield_max = NULL,
  dividend_filter = "all",
  market_cap_min = 1e+09,
  market_cap_max = 1e+11,
  top_n = 50,
  strike_threshold_pct = CASH_SECURED_PUTS_CONFIG$strike_threshold_pct,
  min_days = CASH_SECURED_PUTS_CONFIG$min_days,
  max_days = CASH_SECURED_PUTS_CONFIG$max_days,
  expiry_month = NULL,
  max_workers = CASH_SECURED_PUTS_CONFIG$max_workers
)
}
\arguments{
\item{dividend_yield_min}{Minimum dividend yield (e.g., 2 for 2%)}

\item{dividend_yield_max}{Maximum dividend yield (e.g., 6 for 6%)}

\item{dividend_filter}{Dividend filter type: "all", "dividend_paying", or "zero_dividend"}

\item{market_cap_min}{Minimum fund net assets in dollars (default 1e9 = $1B)}

\item{market_cap_max}{Maximum fund net assets in dollars (default 100e9 = $100B)}

\item{top_n}{Maximum number of ETFs to analyze, sorted by volume (default 50)}

\item{strike_threshold_pct}{Minimum strike as % of current price (default from config)}

\item{min_days}{Minimum days to expiry (default from config)}

\item{max_days}{Maximum days to expiry (default from config)}

\item{expiry_month}{Target expiry month (1-12, NULL for any)}

\item{max_workers}{Number of parallel workers (default from config)}
}
\value{
Tibble with cash-secured put opportunities sorted by annualized return
}
\description{
Analyzes ETFs from Yahoo Finance screener for cash-secured put opportunities.
Uses yfscreen package to filter ETFs by market cap, dividend characteristics,
and trading volume before analyzing put options.
}
\details{
Key differences from aristocrats variant:
- Dynamic ETF filtering based on real-time criteria
- Customizable dividend and market cap filters
- Volume-sorted selection for optimal liquidity
- Works with any ETF universe (dividend-paying, zero-dividend, or all)
}
\examples{
\dontrun{
  # Analyze default set of ETFs
  results <- analyze_etf_cash_secured_puts_yfscreen()

  # Analyze large-cap dividend-paying ETFs
  results <- analyze_etf_cash_secured_puts_yfscreen(
    dividend_filter = "dividend_paying",
    dividend_yield_min = 2,
    dividend_yield_max = 6,
    market_cap_min = 10e9,
    top_n = 30
  )

  # Analyze zero-dividend ETFs for maximum capital gains focus
  results <- analyze_etf_cash_secured_puts_yfscreen(
    dividend_filter = "zero_dividend",
    market_cap_min = 5e9,
    strike_threshold_pct = 0.90,
    top_n = 25
  )
}
}
