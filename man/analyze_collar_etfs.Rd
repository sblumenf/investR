% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_collar_analysis.R
\name{analyze_collar_etfs}
\alias{analyze_collar_etfs}
\title{Analyze liquid ETFs for collar opportunities}
\usage{
analyze_collar_etfs(
  min_market_cap = COLLAR_CONFIG$min_market_cap,
  min_avg_volume = COLLAR_CONFIG$min_avg_volume,
  target_days = 300,
  strike_adjustment_pct = 0,
  limit = NULL,
  max_workers = COLLAR_CONFIG$max_workers
)
}
\arguments{
\item{min_market_cap}{Minimum market capitalization (default from config)}

\item{min_avg_volume}{Minimum average daily volume (default from config)}

\item{target_days}{Target days to expiry (finds closest match, default 300)}

\item{strike_adjustment_pct}{Strike adjustment as decimal (0 = ATM, 0.05 = 5% above)}

\item{limit}{Optional limit on number of ETFs to analyze (for testing)}

\item{max_workers}{Number of parallel workers}
}
\value{
Tibble with all opportunities sorted by annualized return
}
\description{
Analyze liquid ETFs for collar opportunities
}
\examples{
\dontrun{
  # Analyze all liquid ETFs
  results <- analyze_collar_etfs()

  # Analyze with custom parameters
  results <- analyze_collar_etfs(target_days = 180, strike_adjustment_pct = 0.05)

  # Analyze with custom filters
  results <- analyze_collar_etfs(min_market_cap = 5e9, min_avg_volume = 5e6)
}
}
